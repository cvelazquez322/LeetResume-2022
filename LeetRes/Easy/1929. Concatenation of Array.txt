class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        newlist = []
        for x in nums:
            newlist.append(x)
        for x in nums:
            newlist.append(x)
                    return newlist